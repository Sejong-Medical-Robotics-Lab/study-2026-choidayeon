실제 로봇을 움직이기 전에 코드만 보고 다음 질문에 답해 봅니다.

이 코드는 어떤 토픽을 구독하는가?
/lidar_points의 메시지 타입은 무엇인가?
왜 모든 LiDAR 점을 사용하지 않고 mask를 사용하는가?
x > 0.2, x < 3.0은 어떤 공간을 의미하는가?
np.abs(y) < 0.4는 왜 필요한가?
가장 가까운 장애물의 거리는 어떤 값으로 계산하는가?
왜 정지 거리는 0.6 m, 재출발 거리는 0.8 m로 다르게 설정했는가?
lidar_callback()과 publish_cmd()의 역할은 어떻게 다른가?
왜 /cmd_vel을 한 번만 보내지 않고 0.1초마다 반복해서 보내는가?
프로그램을 Ctrl+C로 종료하면 어떤 정지 코드가 실행되는가?
코드의 흐름을 설명할 수 있다면 실기체에서 실행합니다.

