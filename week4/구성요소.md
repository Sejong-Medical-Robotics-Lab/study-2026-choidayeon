## 미션 ① Go2 구성 요소 지도 그리기
```
참고 구성: 다리×4(FR·FL·RR·RL) / 다리당 관절×3(hip·thigh·calf, 총 12 DOF) /
IMU(imu 링크) / LiDAR(lidar_link, 실기체 L1 대응) / 몸통(base) /
sport mode 관제 노드(go2_sim) / SportClient API / go2_edu_interfaces(메시지)
다리 대응: hip(고관절 좌우 벌림) — thigh(고관절 앞뒤) — calf(무릎)
관절 순서(DDS=/go2/lowstate 동일): FR(0~2) → FL(3~5) → RR(6~8) → RL(9~11)
```
